export interface City {
    code: number,
    name: string,
    stdCode: string,
    year: string
}

export interface CreateCity {
    name: string,
}

export interface UpdateCity {
    name: string,
}