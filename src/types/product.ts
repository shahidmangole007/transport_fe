export interface Product {
    code: number,
    name: string,
    stdCode: string,
    year: string
}

export interface CreateProduct {
    name: string,
}

export interface UpdateProduct {
    name: string,
}